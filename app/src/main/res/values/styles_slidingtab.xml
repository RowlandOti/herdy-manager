<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- TextView styles -->
    <!-- TextView styles Normal -->
    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">true</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/color_list_text_selector</item>
    </style>
    <!-- TextView styles Bold -->
    <style name="CustomTabTextAppearance.Bold" parent="CustomTabTextAppearance">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">@color/app_color_white</item>
        <item name="android:shadowRadius">2</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">0</item>
    </style>

    <style name="Widget.App.Tab">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">@dimen/tab_height</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">@dimen/tab_padding_horizontal</item>
        <item name="android:paddingRight">@dimen/tab_padding_horizontal</item>
    </style>

    <!-- Widget SlidingTabStripLayout styles -->
    <style name="Widget.App.SlidingTabStripLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/app_color_primary_dark</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
        <item name="android:textAppearance">@style/CustomTabTextAppearance</item>
        <item name="android:background">@color/app_color_white</item>
        <item name="tabGravity">fill</item>
        <item name="tabMode">scrollable</item>
    </style>
</resources>