<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.herdy.manager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="co.herdy.manager.presentation.ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--Main-Activity-->
        <activity
            android:name="co.herdy.manager.presentation.userfeature.view.activity.UserListActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--User-Feature-->
        <activity
            android:name="co.herdy.manager.presentation.userfeature.view.activity.UserDetailsActivity"
            android:label="@string/activity_title_user_details"></activity>

    </application>

</manifest>
